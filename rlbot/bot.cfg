[Locations]
# Nom affiché en jeu
name = Botrl-Live

# Script Python du bot (DANS LE MÊME DOSSIER que bot.cfg)
python_file = ./my_bot.py

# Apparence du bot (commenté pour éviter les erreurs)
# looks_config = ./appearance.cfg
# looks_path = ./appearance.cfg

# Optionnels (commenté si vide)
# requirements_file =
# logo_file =
use_virtual_environment = False
use_virtualenv = False

[Bot Parameters]
# (laisser vide ou ajouter tes params)

[Details]
developer = FluxDown
description = RLBot bridge for Botrl; reloads latest_policy.pt.
github = https://github.com/FluxDown/Botrl
language = Python
tags = 1v1
